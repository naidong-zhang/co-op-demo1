The *serial* branch implements serialization.

The *parceler* branch implements parcelization, but it only works with Java data class, not Kotlin data class.

The *main* branch is the same with the *serial* branch.
